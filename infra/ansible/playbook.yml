---
- name: Provision VOKO server
  hosts: all
  become: true

  vars_files:
    - vars/main.yml
    - vars/vault.yml

  roles:
    - role: base
      tags: [base]
    - role: firewall
      tags: [firewall]
    - role: docker
      tags: [docker]
    - role: nginx
      tags: [nginx]
    - role: certbot
      tags: [certbot]
    - role: app
      tags: [app]
