all:
  hosts:
    voko_server:
      ansible_host: leden.vokoutrecht.nl
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/id_rsa

  children:
    production:
      hosts:
        voko_server:
    acceptance:
      hosts:
        voko_server:
